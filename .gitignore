# Agent / assistant tooling — never checked in.
# This is the single place tool names appear; the policy checker itself is
# vendor-neutral and simply enforces that nothing ignored here is tracked.
CLAUDE.md
CLAUDE.local.md
.claude/
.claude.json
.claude.local.json
GEMINI.md
.gemini/
AGENTS.md
.codex/
.openai/
.github/copilot-instructions.md
.copilot/
.cursor/
.cursorrules
.aider*
.continue/
.windsurfrules
.mcp.json

# Local env / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Build output
/bin/
/dist/
/llama-herd
*.so
*.a
*.o
*.test
*.exe

# Model weights & artifacts
*.gguf
*.onnx
*.safetensors
/models/
/weights/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
.internal-patterns

# Local scratch. Never committed.
#
# testing/ in particular holds provider-specific deployment and test objects — container
# group definitions, provider configs, node setup — which name infrastructure that must not
# appear in a repo intended to go public.
testing/
scripts/local/
dev/
