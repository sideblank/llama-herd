# Agent / assistant tooling — never checked in.
# This is the single place tool names appear; the policy checker itself is
# vendor-neutral and simply enforces that nothing ignored here is tracked.
CLAUDE.md
CLAUDE.local.md
.claude/
.claude.json
.claude.local.json
GEMINI.md
.gemini/
AGENTS.md
.codex/
.openai/
.github/copilot-instructions.md
.copilot/
.cursor/
.cursorrules
.aider*
.continue/
.windsurfrules
.mcp.json

# Local env / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Build output
/bin/
/dist/
*.so
*.a
*.o
*.test
*.exe

# Model weights & artifacts
*.gguf
*.onnx
*.safetensors
/models/
/weights/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/
.internal-patterns

# Local scratch scripts. scripts/ itself is tracked — CI runs check-commits.sh and
# check-leaks.sh from there — so only this subdirectory is ignored.
scripts/local/
dev/
